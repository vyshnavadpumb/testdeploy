# Trigger deployment test
services:
  web:
    image: nginx:latest
    ports:
      - "${APP_PORT:-80}:80"
    volumes:
      - ./html:/usr/share/nginx/html
    restart: always
    environment:
      - NODE_ENV=${NODE_ENV:-staging}

networks:
  default:
    name: myapp-network
